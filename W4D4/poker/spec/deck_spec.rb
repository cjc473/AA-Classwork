require "deck"

describe Deck do
  subject(:deck) { Deck.new }
  
  describe "#initialize" do
    it "sets sleeve to Desk::create_deck" do
      # expects(deck.create_deck).to 
    end

    it "sets size to sleeve length" do
      
    end
  end


end